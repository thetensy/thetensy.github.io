---
// 購物車摘要組件
// 顯示目前選擇的項目和總價
---

<aside class="cart-panel">
  <div class="cart-card">
    <!-- 目前項目 -->
    <div class="cart-current" id="cartCurrent">
      <div class="cart-header">
        <div class="cart-icon-wrap">🎨</div>
        <div>
          <div class="cart-product-name" id="currentProductName">Logo 設計</div>
          <div class="cart-style-name" id="currentStyleName">請選擇風格</div>
        </div>
      </div>
      
      <div class="cart-details" id="cartDetails">
        <div class="cart-row">
          <span class="cart-label">方案</span>
          <span class="cart-value" id="currentPlan">基礎版</span>
        </div>
        <div class="cart-row">
          <span class="cart-label">價格</span>
          <span class="cart-value" id="currentPrice">NT$ 990</span>
        </div>
        <div class="cart-row">
          <span class="cart-label">交期</span>
          <span class="cart-value" id="currentDays">3 個工作天</span>
        </div>
        <div class="cart-row">
          <span class="cart-label">修改次數</span>
          <span class="cart-value" id="currentRevisions">1 次</span>
        </div>
        <div class="cart-row">
          <span class="cart-label">交件格式</span>
          <span class="cart-value" id="currentFormats">PNG</span>
        </div>
      </div>
    </div>
    
    <!-- 購物車項目（多件時顯示） -->
    <div class="cart-items" id="cartItems" style="display: none;">
      <!-- 動態填入 -->
    </div>
    
    <!-- 分隔線 -->
    <div class="cart-divider"></div>
    
    <!-- 總計 -->
    <div class="cart-total">
      <span class="total-label">總計</span>
      <span class="total-price">
        <span class="currency">NT$</span>
        <span class="amount" id="totalAmount">990</span>
      </span>
    </div>
    
    <!-- 行動按鈕 -->
    <div class="cart-actions">
      <button class="btn btn-primary btn-lg" id="addToCartBtn" type="button">
        🛒 加入購物車
      </button>
      <button class="btn btn-ghost" id="downloadQuoteBtn" type="button">
        📄 下載報價單
      </button>
    </div>
    
    <!-- 急件提示 -->
    <button class="rush-order-btn" id="rushOrderBtn" type="button">
      ⚡ 急件？點此詢問
    </button>
    
    <!-- 信任徽章 -->
    <div class="trust-badges">
      <div class="trust-badge">
        <span class="badge-icon">✅</span>
        <span>500+ 完成設計</span>
      </div>
      <div class="trust-badge">
        <span class="badge-icon">⭐</span>
        <span>98% 滿意度</span>
      </div>
    </div>
  </div>
  
  <!-- Upsell 提示 -->
  <div class="upsell-card">
    <div class="upsell-content">
      <span class="upsell-icon">💎</span>
      <div>
        <div class="upsell-title">需要完整品牌規範？</div>
        <div class="upsell-desc">CIS/VI 企業識別設計 $9,900 起</div>
      </div>
    </div>
    <a href="/services/cis" class="upsell-link">了解更多 →</a>
  </div>
</aside>

<!-- 急件 Modal -->
<div class="modal-overlay" id="rushModal" style="display: none;">
  <div class="modal-content">
    <button class="modal-close" id="closeRushModal">✕</button>
    <div class="modal-icon">⚡</div>
    <h3 class="modal-title">急件服務</h3>
    <p class="modal-text">
      急件費用依實際情況報價，<br>
      請透過 LINE 與我們聯繫！
    </p>
    <a href="https://line.me/ti/p/@tensy" target="_blank" class="btn btn-line btn-lg">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
        <path d="M19.365 9.863c.349 0 .63.285.63.631 0 .345-.281.63-.63.63H17.61v1.125h1.755c.349 0 .63.283.63.63 0 .344-.281.629-.63.629h-2.386c-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63h2.386c.346 0 .627.285.627.63 0 .349-.281.63-.63.63H17.61v1.125h1.755zm-3.855 3.016c0 .27-.174.51-.432.596-.064.021-.133.031-.199.031-.193 0-.378-.104-.481-.281l-2.432-3.308v2.962c0 .349-.282.63-.63.63-.345 0-.627-.281-.627-.63V8.108c0-.27.173-.51.43-.595.06-.023.136-.033.194-.033.195 0 .375.104.478.281l2.439 3.308V8.108c0-.345.282-.63.63-.63.346 0 .628.285.628.63v4.771zm-5.741 0c0 .349-.282.63-.631.63-.345 0-.627-.281-.627-.63V8.108c0-.345.282-.63.63-.63.346 0 .628.285.628.63v4.771zm-2.466.63H4.917c-.345 0-.63-.281-.63-.63V8.108c0-.345.285-.63.63-.63.349 0 .63.285.63.63v4.141h1.756c.348 0 .629.283.629.63 0 .344-.282.629-.629.629M24 10.314C24 4.943 18.615.572 12 .572S0 4.943 0 10.314c0 4.811 4.27 8.842 10.035 9.608.391.082.923.258 1.058.59.12.301.079.766.038 1.08l-.164 1.02c-.045.301-.24 1.186 1.049.645 1.291-.539 6.916-4.078 9.436-6.975C23.176 14.393 24 12.458 24 10.314"/>
      </svg>
      LINE 聯繫我們
    </a>
  </div>
</div>

<style>
  .cart-panel {
    position: sticky;
    top: calc(var(--header-height) + var(--space-6));
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }
  
  .cart-card {
    background: var(--white);
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    box-shadow: var(--shadow);
  }
  
  .cart-header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-5);
    padding-bottom: var(--space-4);
    border-bottom: 1px solid var(--gray-100);
  }
  
  .cart-icon-wrap {
    width: 44px;
    height: 44px;
    background: var(--cream);
    border-radius: var(--radius);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
  }
  
  .cart-product-name {
    font-size: 16px;
    font-weight: 700;
    color: var(--gray-800);
  }
  
  .cart-style-name {
    font-size: 13px;
    color: var(--gray-500);
  }
  
  .cart-details {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }
  
  .cart-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .cart-label {
    font-size: 14px;
    color: var(--gray-500);
  }
  
  .cart-value {
    font-size: 14px;
    font-weight: 600;
    color: var(--gray-800);
  }
  
  .cart-divider {
    height: 1px;
    background: var(--gray-200);
    margin: var(--space-5) 0;
  }
  
  .cart-total {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-5);
  }
  
  .total-label {
    font-size: 16px;
    font-weight: 600;
    color: var(--gray-700);
  }
  
  .total-price {
    display: flex;
    align-items: baseline;
    gap: 4px;
  }
  
  .total-price .currency {
    font-size: 16px;
    color: var(--gray-500);
  }
  
  .total-price .amount {
    font-size: 32px;
    font-weight: 900;
    color: var(--primary);
  }
  
  .cart-actions {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }
  
  .rush-order-btn {
    width: 100%;
    margin-top: var(--space-3);
    padding: var(--space-3);
    background: var(--cream);
    border: 1px dashed var(--gray-300);
    border-radius: var(--radius);
    color: var(--gray-600);
    font-size: 13px;
    font-weight: 500;
    transition: all 0.2s ease;
  }
  
  .rush-order-btn:hover {
    background: var(--cream-dark);
    border-color: var(--gray-400);
    color: var(--gray-700);
  }
  
  .trust-badges {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    margin-top: var(--space-5);
    padding-top: var(--space-4);
    border-top: 1px solid var(--gray-100);
  }
  
  .trust-badge {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 12px;
    color: var(--gray-500);
  }
  
  .badge-icon {
    font-size: 14px;
  }
  
  /* Upsell Card */
  .upsell-card {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
    border-radius: var(--radius-lg);
    padding: var(--space-5);
    color: white;
  }
  
  .upsell-content {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    margin-bottom: var(--space-3);
  }
  
  .upsell-icon {
    font-size: 24px;
  }
  
  .upsell-title {
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 2px;
  }
  
  .upsell-desc {
    font-size: 13px;
    opacity: 0.9;
  }
  
  .upsell-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    font-size: 13px;
    font-weight: 600;
    color: white;
    opacity: 0.9;
    transition: opacity 0.2s ease;
  }
  
  .upsell-link:hover {
    opacity: 1;
  }
  
  /* Modal */
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
    backdrop-filter: blur(4px);
  }
  
  .modal-content {
    position: relative;
    background: var(--white);
    border-radius: var(--radius-xl);
    padding: var(--space-10);
    max-width: 400px;
    width: 90%;
    text-align: center;
    animation: slideUp 0.3s ease;
  }
  
  .modal-close {
    position: absolute;
    top: var(--space-4);
    right: var(--space-4);
    width: 32px;
    height: 32px;
    border: none;
    background: var(--gray-100);
    border-radius: 50%;
    color: var(--gray-500);
    font-size: 16px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  
  .modal-close:hover {
    background: var(--gray-200);
    color: var(--gray-700);
  }
  
  .modal-icon {
    font-size: 48px;
    margin-bottom: var(--space-4);
  }
  
  .modal-title {
    font-size: 24px;
    font-weight: 900;
    color: var(--gray-800);
    margin-bottom: var(--space-3);
  }
  
  .modal-text {
    font-size: 15px;
    color: var(--gray-600);
    line-height: 1.7;
    margin-bottom: var(--space-6);
  }
  
  @media (max-width: 1024px) {
    .cart-panel {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      top: auto;
      padding: var(--space-4);
      background: var(--white);
      border-top: 1px solid var(--gray-200);
      box-shadow: 0 -4px 20px rgba(0,0,0,0.1);
      z-index: 90;
    }
    
    .cart-card {
      box-shadow: none;
      padding: 0;
    }
    
    .cart-current,
    .cart-items,
    .trust-badges,
    .upsell-card {
      display: none;
    }
    
    .cart-divider {
      display: none;
    }
    
    .cart-total {
      margin-bottom: var(--space-3);
    }
    
    .cart-actions {
      flex-direction: row;
    }
    
    .cart-actions .btn {
      flex: 1;
    }
    
    .rush-order-btn {
      display: none;
    }
  }
</style>

<script>
  // 急件 Modal
  const rushBtn = document.getElementById('rushOrderBtn');
  const rushModal = document.getElementById('rushModal');
  const closeBtn = document.getElementById('closeRushModal');
  
  rushBtn?.addEventListener('click', () => {
    rushModal!.style.display = 'flex';
  });
  
  closeBtn?.addEventListener('click', () => {
    rushModal!.style.display = 'none';
  });
  
  rushModal?.addEventListener('click', (e) => {
    if (e.target === rushModal) {
      rushModal.style.display = 'none';
    }
  });
</script>
