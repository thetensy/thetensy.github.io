---
// 方案選擇組件
// 這裡決定價格、交期、修改次數

interface Plan {
  name: string;
  price: number;
  deliveryDays: number;
  revisions: number;
  formats: string[];
  description: string;
  recommended?: boolean;
  features?: string[];
}

interface Props {
  plans: Record<string, Plan>;
  selectedPlan?: string;
  productName: string;
  serviceNote?: string;
}

const { plans, selectedPlan = 'basic', productName, serviceNote } = Astro.props;
---

<div class="plan-selector">
  <h3 class="plan-title">
    <span class="plan-icon">📦</span>
    選擇方案
  </h3>
  
  {serviceNote && (
    <p class="service-note">
      <span class="note-icon">💡</span>
      {serviceNote}
    </p>
  )}
  
  <div class="plan-options">
    {Object.entries(plans).map(([key, plan]) => (
      <label class:list={['plan-option', { selected: key === selectedPlan, recommended: plan.recommended }]}>
        <input 
          type="radio" 
          name="plan" 
          value={key}
          checked={key === selectedPlan}
          data-price={plan.price}
          data-days={plan.deliveryDays}
          data-revisions={plan.revisions}
          data-formats={plan.formats.join(', ')}
        />
        
        {plan.recommended && <span class="recommended-badge">推薦</span>}
        
        <div class="plan-header">
          <span class="plan-name">{plan.name}</span>
          <span class="plan-price">
            <span class="currency">NT$</span>
            <span class="amount">{plan.price.toLocaleString()}</span>
          </span>
        </div>
        
        <div class="plan-details">
          <div class="plan-detail">
            <span class="detail-icon">📅</span>
            <span>{plan.deliveryDays} 個工作天</span>
          </div>
          <div class="plan-detail">
            <span class="detail-icon">🔄</span>
            <span>{plan.revisions} 次免費修改</span>
          </div>
          <div class="plan-detail">
            <span class="detail-icon">📁</span>
            <span>{plan.formats.join(' + ')}</span>
          </div>
        </div>
        
        {plan.features && (
          <div class="plan-features">
            {plan.features.map(feature => (
              <span class="feature-tag">✓ {feature}</span>
            ))}
          </div>
        )}
        
        <p class="plan-desc">{plan.description}</p>
      </label>
    ))}
  </div>
</div>

<style>
  .plan-selector {
    margin-bottom: var(--space-6);
  }
  
  .plan-title {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 16px;
    font-weight: 700;
    color: var(--gray-800);
    margin-bottom: var(--space-3);
  }
  
  .plan-icon {
    font-size: 18px;
  }
  
  .service-note {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 14px;
    color: var(--teal);
    background: rgba(0, 180, 160, 0.08);
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius);
    margin-bottom: var(--space-4);
  }
  
  .note-icon {
    font-size: 16px;
  }
  
  .plan-options {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-4);
  }
  
  .plan-option {
    position: relative;
    display: flex;
    flex-direction: column;
    padding: var(--space-5);
    background: var(--white);
    border: 2px solid var(--gray-200);
    border-radius: var(--radius-lg);
    cursor: pointer;
    transition: all 0.3s ease;
  }
  
  .plan-option input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }
  
  .plan-option:hover {
    border-color: var(--gray-300);
    transform: translateY(-2px);
    box-shadow: var(--shadow);
  }
  
  /* 選中時的可愛外光暈特效 */
  .plan-option.selected {
    border-color: var(--primary);
    background: rgba(46, 59, 107, 0.02);
    box-shadow: 
      0 0 0 3px rgba(46, 59, 107, 0.15),
      0 0 20px rgba(46, 59, 107, 0.1),
      0 4px 12px rgba(46, 59, 107, 0.15);
    transform: translateY(-2px);
  }
  
  .plan-option.recommended {
    border-color: var(--teal);
  }
  
  .plan-option.recommended.selected {
    border-color: var(--teal);
    background: rgba(0, 180, 160, 0.02);
    box-shadow: 
      0 0 0 3px rgba(0, 180, 160, 0.2),
      0 0 20px rgba(0, 180, 160, 0.15),
      0 4px 12px rgba(0, 180, 160, 0.2);
  }
  
  .recommended-badge {
    position: absolute;
    top: -10px;
    left: var(--space-4);
    background: var(--teal);
    color: white;
    font-size: 11px;
    font-weight: 700;
    padding: 4px 10px;
    border-radius: var(--radius-full);
  }
  
  .plan-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: var(--space-4);
    gap: var(--space-2);
  }
  
  .plan-name {
    font-size: 18px;
    font-weight: 700;
    color: var(--gray-800);
    flex-shrink: 0;
  }
  
  .plan-price {
    display: flex;
    align-items: baseline;
    gap: 2px;
  }
  
  .plan-price .currency {
    font-size: 12px;
    color: var(--gray-500);
  }
  
  .plan-price .amount {
    font-size: 22px;
    font-weight: 900;
    color: var(--primary);
  }
  
  .plan-details {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    margin-bottom: var(--space-3);
  }
  
  .plan-detail {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 13px;
    color: var(--gray-600);
  }
  
  .detail-icon {
    font-size: 14px;
    width: 20px;
    text-align: center;
  }
  
  .plan-features {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-3);
  }
  
  .feature-tag {
    font-size: 11px;
    color: var(--teal);
    background: rgba(0, 180, 160, 0.1);
    padding: 3px 8px;
    border-radius: var(--radius-full);
  }
  
  .plan-desc {
    font-size: 13px;
    color: var(--gray-500);
    margin-top: auto;
    padding-top: var(--space-3);
    border-top: 1px solid var(--gray-100);
  }
</style>
