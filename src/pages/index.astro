---
// 添喜設計首頁
// 簡潔的品牌介紹與入口

import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
---

<Layout title="平價設計服務">
  <Header />
  
  <main class="hero">
    <div class="hero-content">
      <span class="hero-badge">🎨 平價設計服務</span>
      <h1 class="hero-title">
        讓每個小店<br>
        都有<span class="highlight">專業形象</span>
      </h1>
      <p class="hero-desc">
        Logo、名片、菜單、社群圖⋯⋯<br>
        選風格、填資料，設計師為你量身打造
      </p>
      
      <div class="hero-cta">
        <a href="/order" class="btn btn-primary btn-lg">
          開始設計 →
        </a>
        <a href="/portfolio" class="btn btn-ghost btn-lg">
          看作品集
        </a>
      </div>
      
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">500+</span>
          <span class="stat-label">完成設計</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-number">98%</span>
          <span class="stat-label">客戶滿意</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-number">$990</span>
          <span class="stat-label">Logo 起</span>
        </div>
      </div>
    </div>
    
    <div class="hero-visual">
      <!-- 之後放作品展示或添添吉祥物 -->
      <div class="visual-placeholder">
        <span>🎨</span>
      </div>
    </div>
  </main>
  
  <!-- 服務項目 -->
  <section class="services container">
    <h2 class="services-title">我們的服務</h2>
    
    <div class="services-grid">
      <a href="/order?product=logo" class="service-card">
        <span class="service-emoji">🎨</span>
        <h3 class="service-name">Logo 設計</h3>
        <p class="service-price">$990 起</p>
      </a>
      
      <a href="/order?product=namecard" class="service-card disabled">
        <span class="service-emoji">💼</span>
        <h3 class="service-name">名片設計</h3>
        <p class="service-price">即將推出</p>
      </a>
      
      <a href="/order?product=menu" class="service-card disabled">
        <span class="service-emoji">🍽️</span>
        <h3 class="service-name">菜單設計</h3>
        <p class="service-price">即將推出</p>
      </a>
      
      <a href="/order?product=social" class="service-card disabled">
        <span class="service-emoji">📱</span>
        <h3 class="service-name">社群圖片</h3>
        <p class="service-price">即將推出</p>
      </a>
      
      <a href="/order?product=dm" class="service-card disabled">
        <span class="service-emoji">📄</span>
        <h3 class="service-name">DM 傳單</h3>
        <p class="service-price">即將推出</p>
      </a>
      
      <a href="/order?product=website" class="service-card">
        <span class="service-emoji">🌐</span>
        <h3 class="service-name">網站設計</h3>
        <p class="service-price">$5,000 起</p>
      </a>
    </div>
  </section>
  
  <!-- 流程說明 -->
  <section class="process">
    <div class="container">
      <h2 class="process-title">簡單三步驟</h2>
      
      <div class="process-steps">
        <div class="step">
          <div class="step-number">1</div>
          <h3 class="step-title">選擇風格</h3>
          <p class="step-desc">瀏覽風格參考，找到你喜歡的方向</p>
        </div>
        
        <div class="step-arrow">→</div>
        
        <div class="step">
          <div class="step-number">2</div>
          <h3 class="step-title">填寫資料</h3>
          <p class="step-desc">告訴我們品牌名稱和偏好</p>
        </div>
        
        <div class="step-arrow">→</div>
        
        <div class="step">
          <div class="step-number">3</div>
          <h3 class="step-title">等待交件</h3>
          <p class="step-desc">設計師為你量身打造</p>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Footer -->
  <footer class="footer">
    <div class="container footer-content">
      <div class="footer-brand">
        <div class="footer-logo">🎨 添喜設計</div>
        <p class="footer-tagline">平價設計服務，讓每個小店都有專業形象</p>
      </div>
      
      <div class="footer-links">
        <a href="https://line.me/ti/p/@tensy" target="_blank">LINE 諮詢</a>
        <a href="/about">關於我們</a>
        <a href="/portfolio">作品集</a>
      </div>
      
      <div class="footer-copy">
        © 2025 添喜設計 TheTensy. All rights reserved.
      </div>
    </div>
  </footer>
</Layout>

<style>
  /* Hero */
  .hero {
    min-height: calc(100vh - var(--header-height));
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: var(--space-12);
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--space-12) var(--space-6);
  }
  
  @media (max-width: 768px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
      padding-top: var(--space-8);
    }
  }
  
  .hero-badge {
    display: inline-block;
    padding: var(--space-2) var(--space-4);
    background: var(--cream-dark);
    border-radius: var(--radius-full);
    font-size: 14px;
    font-weight: 600;
    color: var(--primary);
    margin-bottom: var(--space-4);
  }
  
  .hero-title {
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 900;
    line-height: 1.2;
    color: var(--gray-800);
    margin-bottom: var(--space-5);
  }
  
  .hero-title .highlight {
    color: var(--accent);
  }
  
  .hero-desc {
    font-size: 18px;
    color: var(--gray-600);
    line-height: 1.7;
    margin-bottom: var(--space-8);
  }
  
  .hero-cta {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-10);
  }
  
  @media (max-width: 768px) {
    .hero-cta {
      justify-content: center;
    }
  }
  
  .hero-stats {
    display: flex;
    align-items: center;
    gap: var(--space-6);
  }
  
  @media (max-width: 768px) {
    .hero-stats {
      justify-content: center;
    }
  }
  
  .stat {
    display: flex;
    flex-direction: column;
  }
  
  .stat-number {
    font-size: 28px;
    font-weight: 900;
    color: var(--primary);
  }
  
  .stat-label {
    font-size: 13px;
    color: var(--gray-500);
  }
  
  .stat-divider {
    width: 1px;
    height: 40px;
    background: var(--gray-300);
  }
  
  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .visual-placeholder {
    width: 400px;
    height: 400px;
    background: var(--white);
    border-radius: var(--radius-xl);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 120px;
    box-shadow: var(--shadow-lg);
  }
  
  @media (max-width: 768px) {
    .hero-visual {
      display: none;
    }
  }
  
  /* Services */
  .services {
    padding: var(--space-12) var(--space-6);
    background: var(--white);
  }
  
  .services-title {
    font-size: 32px;
    font-weight: 900;
    text-align: center;
    color: var(--gray-800);
    margin-bottom: var(--space-10);
  }
  
  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: var(--space-5);
    max-width: 900px;
    margin: 0 auto;
  }
  
  .service-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--space-6);
    background: var(--cream);
    border-radius: var(--radius-lg);
    text-align: center;
    transition: all 0.2s ease;
  }
  
  .service-card:not(.disabled):hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow);
  }
  
  .service-card.disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }
  
  .service-emoji {
    font-size: 40px;
    margin-bottom: var(--space-3);
  }
  
  .service-name {
    font-size: 16px;
    font-weight: 700;
    color: var(--gray-800);
    margin-bottom: var(--space-1);
  }
  
  .service-price {
    font-size: 14px;
    color: var(--gray-500);
  }
  
  /* Process */
  .process {
    padding: var(--space-12) var(--space-6);
    background: var(--primary);
    color: white;
  }
  
  .process-title {
    font-size: 32px;
    font-weight: 900;
    text-align: center;
    margin-bottom: var(--space-10);
  }
  
  .process-steps {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: var(--space-6);
    max-width: 800px;
    margin: 0 auto;
  }
  
  @media (max-width: 768px) {
    .process-steps {
      flex-direction: column;
      align-items: center;
    }
    
    .step-arrow {
      transform: rotate(90deg);
    }
  }
  
  .step {
    text-align: center;
    max-width: 200px;
  }
  
  .step-number {
    width: 48px;
    height: 48px;
    background: var(--accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    font-weight: 900;
    margin: 0 auto var(--space-4);
  }
  
  .step-title {
    font-size: 18px;
    font-weight: 700;
    margin-bottom: var(--space-2);
  }
  
  .step-desc {
    font-size: 14px;
    opacity: 0.8;
    line-height: 1.5;
  }
  
  .step-arrow {
    font-size: 24px;
    opacity: 0.5;
    margin-top: var(--space-3);
  }
  
  /* Footer */
  .footer {
    padding: var(--space-10) var(--space-6);
    background: var(--gray-800);
    color: white;
  }
  
  .footer-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-6);
  }
  
  .footer-logo {
    font-size: 20px;
    font-weight: 900;
  }
  
  .footer-tagline {
    font-size: 14px;
    opacity: 0.7;
  }
  
  .footer-links {
    display: flex;
    gap: var(--space-6);
  }
  
  .footer-links a {
    font-size: 14px;
    opacity: 0.8;
    transition: opacity 0.2s ease;
  }
  
  .footer-links a:hover {
    opacity: 1;
  }
  
  .footer-copy {
    font-size: 12px;
    opacity: 0.5;
  }
</style>
