# 添喜設計 - Auth Worker 設定
# 使用方式：
# 1. cd workers
# 2. wrangler deploy

name = "tensy-auth"
main = "auth.ts"
compatibility_date = "2024-01-01"

# 路由設定（部署到你的網域後設定）
# routes = [
#   { pattern = "thetensy.com/api/auth/*", zone_name = "thetensy.com" }
# ]

# 環境變數（非敏感）
[vars]
LINE_CHANNEL_ID = "2008810910"

# 敏感資訊請用 wrangler secret put 設定：
# wrangler secret put LINE_CHANNEL_SECRET
# wrangler secret put JWT_SECRET
